"""Phase 3 home — feedback control.

The Dynamixel XL430 runs its own internal position PID, so the host-side work
here will mostly be (a) trajectory-following PID on top of joint targets and
(b) ergonomic tuning utilities. Intentionally empty until Phase 3.
"""
