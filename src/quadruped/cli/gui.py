"""Dear PyGui slider app — Phase 1.5 deliverable, fleshed out in Landing 2.

Stub for now; raises a clear message if invoked before Landing 2 lands.
"""
from __future__ import annotations


def main() -> int:
    raise SystemExit(
        "quad-gui is part of Landing 2 (Dear PyGui slider app). "
        "For now use `quad-send-foot`, `quad-jog`, or `quad-view`."
    )


if __name__ == "__main__":
    main()
