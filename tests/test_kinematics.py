"""Kinematics tests.

Empty until the kinematics package is reimplemented. Recommended first test
(matches what was here before): IK → FK round-trip — sample N random targets
inside the reachable shell, solve IK, forward-evaluate, assert the foot
landed within tolerance of the requested target.
"""
